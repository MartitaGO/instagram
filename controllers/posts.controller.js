export const lookPhotos = async(texto) => {
    // TODO llamar servicio de busqueda en la base de datos usando como parámetro el texto
    // WHERE desciption LIKE '%texto%'

    //Luego de obtener el listado por el servicio devolverlo con un return

    return texto //Return de ejemplo NO SIRVE
}